# SS4Rec Dependencies
# NOTE: s5-pytorch requires Python 3.10+ and PyTorch 2.0+
# Core ML framework dependencies (install torch first to resolve build dependencies)
torch>=2.0.0
numpy>=1.21.0,<2.0.0
pandas>=1.3.0
scipy>=1.9.0
scikit-learn>=1.1.0

# Build dependencies (needed before mamba-ssm compilation)
ninja>=1.10.0
packaging>=21.0
wheel>=0.37.0
setuptools>=60.0

# State Space Model dependencies (mamba-ssm requires torch to be available during build)
causal-conv1d>=1.2.0
mamba-ssm==2.2.2
s5-pytorch>=0.2.1

# RecBole framework (REQUIRED for official SS4Rec implementation)
recbole==1.2.0

# Experiment tracking and monitoring
wandb>=0.15.0
tensorboard>=2.10.0

# Data processing
tqdm>=4.64.0
pyyaml>=6.0
diskcache>=5.4.0

# Evaluation and metrics
matplotlib>=3.5.0
seaborn>=0.11.0

# Development dependencies
pytest>=7.0.0
black>=22.0.0
isort>=5.10.0
mypy>=0.900
ruff>=0.0.200

# Existing project dependencies (maintain compatibility)
streamlit>=1.25.0
plotly>=5.10.0
fastapi>=0.95.0
uvicorn>=0.20.0